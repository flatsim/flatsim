eval "$(devbox generate direnv --print-envrc)"

export ENVNAME=cmake
export TOP_HEAD=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" &> /dev/null && pwd)

unset GITHUB_TOKEN

export_alias _b "$TOP_HEAD/run.sh b --arrow"
export_alias _m "$TOP_HEAD/run.sh m"
