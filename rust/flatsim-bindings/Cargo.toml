[package]
name = "flatsim-bindings"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
# This crate will contain bindings to C++ libraries like Muli physics engine
# For now, it's a placeholder for future bindgen-generated code

[build-dependencies]
# bindgen will be added later when we need to bind to Muli
# bindgen = "0.69"
